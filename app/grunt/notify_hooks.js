'use strict';

module.exports = function(grunt, options) {
    return {
        options: {
            enabled: true,
            max_jshint_notifications: 5 // max jshint notifications
        }
    }
};